import ApiUtils from "@/utils/api/axiosHelper";
interface Utils {
  handleLogin: () => void;
}

export default function LoginUtils(): Utils {
  const handleLogin = () => {};

  return { handleLogin };
}
